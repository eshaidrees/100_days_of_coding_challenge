"use strict";
function converting(str) {
    let upperCaceStr = str.toUpperCase(); // convert to Upper Case
    let lowerCaseStr = str.toLowerCase(); // convert to lower Case
    console.log("UpperCase:", upperCaceStr, "LowerCase:", lowerCaseStr);
}
converting("Alina");
