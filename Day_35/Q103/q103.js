"use strict";
function randomBoolean() {
    // If the random number is greater than to 0.5, return true, otherwise return false
    return Math.random() > 0.5;
}
console.log(randomBoolean()); // Outputs will be either true or false 
