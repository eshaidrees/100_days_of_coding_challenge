// This function adds two numbers and returns the sum
function numbers(num1, num2) {
    var addNumber = num1 + num2;
    return addNumber;
}
;
// calling the functon with two number and loggin the result
console.log(numbers(5, 10)); // 15
